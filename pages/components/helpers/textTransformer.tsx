// this should be responsible to getting the text from editor.tsx and letting it know how to present the text based on the text classification.

class transform {
	constructor() {}
}

const textTransformer = (themeData: object, editorText: string) => {
	console.log(themeData);
	console.log(editorText);
};

export default textTransformer;
